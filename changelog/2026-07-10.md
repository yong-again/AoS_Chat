# 2026-07-10 수정 이력

## 1. RAG 파이프라인 오프라인 평가 하네스 추가 + 베이스라인 측정

**증상(배경)**: 파이프라인 수정이 누적되면서 회귀 여부를 사람이 채팅으로 일일이
확인해야 했고, 정량적인 성능 기준선이 없었다.

**수정**: `data/eval/rag_eval_qa.json`(19문항: faction 6, rule 4, balance 4,
spearhead 3, other 2 — 각 문항에 기대 답변·근거 문서·기대 라우팅·필수 키워드 포함)
기반의 평가 하네스 `scripts/eval_rag.py`를 추가.

- app.py를 bare 모드로 import해 **실제 파이프라인 코드**(라우팅 → 4중 안전장치 →
  키워드 추출 → 듀얼 임베딩 + BM25/RRF 하이브리드 → 보강/폴백 → 답변 생성)를
  단일 턴으로 재현 실행 (사이드바 기본값 조건: 패치 포함, 과거 GHB 제외)
- 측정 지표 4종: ① 라우팅 정확도 ② 검색 적중률(영문 키워드가 컨텍스트에 포함)
  ③ 답변 키워드 커버리지 ④ LLM 심사(flash-lite, 기대 답변 대비
  correct/partial/incorrect)
- 레이트 리밋 대비 클라이언트 레벨 재시도(5→60초 백오프), 문항별 중간 저장
- 실행: `python -m scripts.eval_rag [--only 1,5] [--no-judge]`
  → 결과: `data/eval/results/eval_<ts>.json`

**검증(베이스라인, eval_20260710_115309)**:

| 지표 | 결과 |
|---|---|
| 라우팅 정확도 | 84.2% (16/19) |
| 검색 적중률(영문 kw) | 82.9% |
| 답변 키워드 커버리지 | 54.8% (한국어 표기 탓 과소 측정 — 아래 참고) |
| LLM 심사 | correct 6 / partial 7 / incorrect 6 |

incorrect 6건의 원인 분석:

- **Q18·Q19 (other_db)**: other_db가 비어 있어(0건) Scourge of Ghyran 질문에
  답변 불가 — 기존 알려진 이슈가 정량 확인됨.
- **Q5**: 유닛 능력 질문에 "규칙" 단어가 있어 라우터가 rule_db 선택(기대
  faction_db) + 키워드 추출이 고유명 오타 생성(Olynder→"Olonder") → 검색 실패.
- **Q16**: 스피어헤드 이름의 **한국어 음차**("놀피스트 클로팩" = Gnawfeast
  Clawpack)가 영문 고유명 사전과 매칭되지 않아 안전장치 미발동 → faction_db
  오라우팅, 다른 인핸스먼트(4+ 워드)로 오답.
- **Q6**: 컨텍스트에 정답(TREESONG)이 있었는데 답변 모델이 다른 주문(CALL FORTH
  THE FOREST)을 선택.
- **Q4**: 기본 데미지·2배 규칙 설명 없이 기대 데미지 계산 결과만 제시(심사 기준
  미달).

**측정 주의**: 답변 키워드 커버리지는 모델이 키워드를 한국어로 번역해 쓰는 경우
(예: 'Crit (Mortal)'→'크릿(모탈)', '모탈 데미지'→'치명적인 피해') 미검출되어
과소평가된다. 정확도 판단은 LLM 심사를 우선 참조.

## 2. 평가로 발견된 신규 버그: 검색 0건일 때 폴백 문서가 컨텍스트에서 유실

**증상**: Q19(other_db 라우팅, 컬렉션 비어 있음)에서 프로브 크로스 DB 폴백이
faction_db 문서를 추가했는데도 최종 컨텍스트가 "관련 문서를 찾을 수 없습니다"로
조립되어 폴백이 무효화됨.

**원인**: app.py의 폴백들(키워드 폴백, 스피어헤드 크로스, 프로브 크로스 DB)은
`results["documents"][0]`/`metadatas`만 앞에 붙이고 `ids`는 갱신하지 않는다.
1차 벡터 검색이 1건이라도 있으면 컨텍스트 조립 루프가 documents를 돌아 문제가
없지만, **0건이면** 조립 가드(`if results["ids"] and len(results["ids"][0]) > 0`)가
False가 되어 폴백으로 추가된 문서 전체가 버려진다.

**수정**: 세 폴백(키워드 폴백, 스피어헤드 크로스, 프로브 크로스 DB) 모두에서
documents/metadatas와 함께 `results["ids"][0]`도 갱신하도록 수정 (app.py).

**검증**: 재현 조건이었던 Q19는 아래 3번(other_db 적재) 이후 1차 검색이 문서를
찾아 해당 경로를 더 타지 않음 — 코드 레벨 수정으로, 세 폴백 지점 모두 ids를
같은 순서로 prepend함을 확인.

## 3. other_db 적재 — Scourge of Ghyran 캠페인 질문 답변 불가 해소 (Q18/Q19)

**증상**: other_db가 0건이라 Scourge of Ghyran 질문(Q18/Q19)에 답변 불가.

**원인**: build_db.py가 PDF 추출본(outputs/)을 `--with-pdf`일 때만 적재하는데,
other_db는 wahapedia 소스가 없어 PDF 추출본이 유일한 소스 — 기본 실행에서 항상
비게 됨.

**수정** (build_db.py):

- other_db는 `--with-pdf` 없이도 항상 `data/outputs/other_db/`에서 적재
- `--only <컬렉션>` 옵션 추가: 지정 컬렉션만 삭제·재적재하고 나머지는 유지
  (전체 재구성 없이 `python build_db.py --only other_db`로 부분 재구성)

추가로 검색 품질 보정 (app.py):

- 라우터 프롬프트의 other_db 설명 구체화(캠페인 이름이 팩션과 함께 나와도
  other_db) + 안전장치 5: `SCOURGE_RE`(영문/음차/번역 표기) 감지 시 other_db 교정
- other_db N_RESULTS 5→25 (캠페인 청크는 캠페인 이름이 전 청크 공통이라 변별력이
  낮아 RRF 컷에서 밀리지 않도록)
- 원문 질문 병행 임베딩 검색 대상에 other_db 추가(키워드 추출이 캠페인 이름만
  남기고 주제를 떨어뜨리는 경우 대비) + 추출 프롬프트에 캠페인 질문 예시 추가

**검증**: `--only other_db` 재구성 후 212청크 적재. Q19 correct,
Q18 incorrect→partial(능력 이름·조건·효과 정답, 전투당 1회 타이밍만 누락).

## 4. 스피어헤드 이름 한국어 음차 매칭 실패 수정 (Q16)

**증상**: "놀피스트 클로팩"(= Gnawfeast Clawpack)처럼 한국어 음차로 쓴
스피어헤드 이름이 영문 고유명 사전과 매칭되지 않아 안전장치 미발동 →
faction_db 오라우팅, 다른 스피어헤드의 인핸스먼트로 오답.

**수정** (app.py):

- 키워드 추출 프롬프트에 규칙 9 추가: 음차 고유명을 공식 영어 철자로 변환하되,
  스피어헤드 이름은 프롬프트에 주입된 공식 명칭 목록(`load_spearhead_names()`)
  에서 골라 그대로 출력
- 안전장치 6(추출 후) 추가: 추출된 검색어에 스피어헤드 고유명이 있으면
  spearhead_db로 교정 — 원문이 음차라 안전장치 3(영문 사전 매칭)이 빗나가도
  추출 단계의 영문 변환 결과로 결정적으로 잡는다

**검증**: Q16 incorrect→correct (Gnawfeast Clawpack 인핸스먼트 정답).

## 5. 유닛 능력 질문의 "규칙" 단어로 인한 rule_db 오라우팅 보정 (Q5)

**증상**: "레이디 올린더 때리려는데 공격이 잘 안 박히는 규칙이 있다던데?" —
"규칙" 단어에 끌려 rule_db 라우팅(기대 faction_db) + 키워드 추출이 고유명 오타
생성(Olynder→"Olonder")으로 검색 실패.

**수정** (app.py): 라우터 프롬프트 규칙 4에 "'규칙' 단어가 있어도 특정 유닛의
고유 능력이면 faction_db" 명시(올린더 예시 포함). 추출 프롬프트 규칙 9(음차→
공식 철자, 임의 발음 표기 금지)와 Lady Olynder 예시로 오타 생성 차단.

**검증**: Q5 라우팅 faction_db 정상화, incorrect→partial(MORTARCH OF GRIEF 핵심
설명 정답, Ward 5+ 부가 정보만 누락).

## 6. 수정 후 재평가 (eval_20260710_123617, 19문항 전체)

| 지표 | 베이스라인 | 수정 후 |
|---|---|---|
| 라우팅 정확도 | 84.2% (16/19) | **89.5% (17/19)** |
| 검색 적중률(영문 kw) | 82.9% | **91.7%** |
| 답변 키워드 커버리지 | 54.8% | **70.6%** |
| LLM 심사 | correct 6 / partial 7 / incorrect 6 | **correct 10 / partial 7 / incorrect 2** |

남은 incorrect 2건 → 원인조사 결과는 아래 7번.

남은 라우팅 실패 2건(Q7 리디플로이, Q10 파워 스루)은 커맨드 질문이 faction_db로
가는 케이스인데, 답변은 둘 다 partial(핵심 내용 정답)로 실해는 크지 않음.

## 7. 남은 incorrect 2건 원인조사 (Q6·Q13) — 둘 다 검색 실패 아님

### Q6: 평가 데이터셋의 기대 답변이 구판 기준 — 파이프라인 답변이 정답

**증상**: "와일드우드 새로 깔 수 있는 주문" 질문에 기대 답변은 TREESONG,
파이프라인 답변은 CALL FORTH THE FOREST → 심사 incorrect.

**원인**: 기대 답변은 PDF 팩션 팩(`outputs/faction_db/faction_pack_sylvaneth.json`,
TREESONG = 와일드우드 3개 제한 설치 주문) 기준으로 작성됐는데, DB는 wahapedia
전용으로 빌드되며 wahapedia는 신판 배틀톰을 반영한다:

- 신판 스펠 로어는 Lore of the Deepwood(JADE THORNS 등)로 교체됐고, 와일드우드
  설치 주문은 Lords of the Clan 포메이션의 **CALL FORTH THE FOREST**(9개 제한)뿐
- 신판의 TREESONG은 **스피어헤드 인핸스먼트**(근접 렌드 +1)로 이름만 같은 전혀
  다른 능력 — 이 청크가 컨텍스트에 걸려 있어 베이스라인 분석 때
  "컨텍스트에 정답(TREESONG)이 있다"고 오판했다

즉 파이프라인은 현행 데이터 기준 정답을 냈고, 틀린 것은 평가 문항이다.

**수정**: 미적용. 평가 데이터셋 Q6의 기대 답변·필수 키워드를 CALL FORTH THE
FOREST 기준으로 갱신 필요.

**검증**: faction_db 전수 스캔 — 와일드우드 설치 TREESONG 청크 0건,
CALL FORTH THE FOREST 청크가 "set up an Awakened Wyldwood ... within 24\"" 명시.

### Q13: balance_db 프롬프트의 엄격한 unit_name 일치 지시 → 확률적 거절

**증상**: 레이디 올린더 포인트 질문에 "해당 유닛의 포인트 정보를 찾을 수
없습니다" 상용구 거절 (검색은 60건 수행).

**원인**: 검색은 완벽 — 정답 청크(320pt, unit_name "Lady Olynder Mortarch of
Grief")가 컨텍스트 문서 1번에 위치, context_recall 1.0. 문제는 답변 생성:
balance_db 시스템 프롬프트의 "[경고] 반드시 unit_name 필드가 **일치**하는
데이터를 찾으세요 … 없다면 '(상용구)'라고 되물어보세요" 지시 때문에, 질문의
한국어 표기("레이디 올린더")가 unit_name("Lady Olynder Mortarch of Grief")과
일치하지 않는다고 판단하는 경우가 temperature 1.0 아래에서 확률적으로 발생.

**검증(대조 실험)**: 동일 컨텍스트·동일 질문으로 3회씩 반복 —
원본 프롬프트: 정답 2회 / 상용구 거절 1회(평가 때 실패 재현).
완화 프롬프트(한국어 표기·축약형이 같은 유닛을 가리키면 사용하라고 명시):
정답 3회 / 거절 0회.

**수정**: 미적용. 후보: balance_db 프롬프트의 일치 경고를 완화 문구로 교체
(+ lookup 모드 temperature 인하 검토).

## 미해결 과제

- 평가 데이터셋 Q6 기대 답변을 신판(CALL FORTH THE FOREST) 기준으로 갱신 (위 7번)
- balance_db 시스템 프롬프트 unit_name 일치 경고 완화 (Q13, 위 7번)
- Q7/Q10: 코어 커맨드(리디플로이, 파워 스루) 질문의 faction_db 오라우팅
- Q18: 캠페인 능력의 발동 타이밍(전투당 1회) 정보 누락 (partial)
- 답변 키워드 커버리지 지표는 한국어 번역 표기 미검출로 과소평가 — 심사 우선 참조
- 심사기 JSON 파싱: 모델이 JSON 뒤에 부연을 붙이는 경우 대응 완료(첫 JSON 객체만
  파싱) — Q8 재심사로 correct 확인
